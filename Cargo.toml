# Cargo.toml (raíz del proyecto)

[workspace]
resolver = "2"
members = [
    "mesa_kernel",
    "tools/mesafs_inject",
]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
lto = true
opt-level = 2
codegen-units = 1
debug = 0
incremental = false